package route_test

/*
func TestNewRoute(t *testing.T) {
	name, match, cat, desc, okay, _func,
		route := routeDummy()

	if route.Name != name {
		t.Fatalf(
			"route.Name == %q != name == %q",
			route.Name, name,
		)
	}

	if route.Match != match {
		t.Fatalf(
			"route.Match == %q != match == %q",
			route.Match, match,
		)
	}

	if route.Cat != cat {
		t.Fatalf(
			"route.Cat == %q != cat == %q",
			route.Cat, cat,
		)
	}

	if route.Desc != desc {
		t.Fatalf(
			"route.Desc == %q != desc == %q",
			route.Desc, desc,
		)
	}

	if val(route.Okay) != val(okay) {
		t.Fatalf(
			"route.Okay == %s != okay == %s",
			val(route.Okay), val(okay),
		)
	}

	if val(route.Func) != val(_func) {
		t.Fatalf(
			"route.Func == %s != _func == %s",
			val(route.Func), val(_func),
		)
	}
}
*/
